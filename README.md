# FastAPI LangChain Agent

FastAPI와 LangChain을 기반으로 구축된 AI 에이전트 프로젝트입니다.  
Poetry를 통해 의존성을 관리하며, **VS Code Dev Containers** 환경에 최적화되어 있습니다.

## 🛠 왜 Dev Container를 사용하나요? (Why Dev Containers?)

이 프로젝트는 **Docker 기반의 VS Code Dev Container** 환경에서 개발하는 것을 권장합니다.

로컬 환경(Mac/Windows)에서 Poetry를 사용할 때,  
**VS Code가 `.venv` 가상환경의 인터프리터 경로를 찾지 못하는 문제**가 종종 발생합니다.

이러한 개발 환경 세팅의 불편함을 없애기 위해, 프로젝트에 필요한 **Python 3.12**와 모든 의존성이 미리 완벽하게 설치된 **Dev Container**를 도입했습니다. 이를 통해 복잡한 경로 설정 없이 **원클릭**으로 즉시 개발을 시작할 수 있습니다.

## ⚠️ 주의 사항: 가상환경(.venv) 호환성

Dev Container를 실행하면 프로젝트 폴더 내에 `.venv` 폴더가 생성됩니다.  
이 폴더는 **Linux(Container) 환경에 맞춰 생성된 바이너리 파일**들을 포함하고 있습니다.

따라서, 컨테이너를 종료하고 로컬(Host) 터미널에서 이 `.venv`를 사용하려고 하면 **실행되지 않습니다.**

* **컨테이너 내부:** `.venv` 정상 작동 (Linux 호환) ✅
* **로컬(Mac/Windows):** 폴더는 보이지만 **실행 불가능** (OS 불일치) ❌  
=> `$(source poetry env activate)` can be available only in the Dev Container

로컬에서 테스트하고 싶다면, 로컬용 가상환경을 별도로 다시 생성해야 합니다.   
하지만 **컨테이너 내부에서 개발 및 실행하는 것을 권장**합니다.

## 📋 사전 요구 사항 (Prerequisites)

프로젝트를 실행하기 위해 다음 도구들이 설치되어 있어야 합니다.

* **Docker Desktop**: [다운로드 링크](https://www.docker.com/products/docker-desktop/)
* **Visual Studio Code**: [다운로드 링크](https://code.visualstudio.com/)
* **VS Code Extension**: `Dev Containers` (Microsoft)

## 🚀 시작하기 (Quick Start)

1. **레포지토리 클론**
   ```bash
   git clone <REPO_URL>
   cd agent
   ```

2. **VS Code 실행**
   ```bash
   code .
   ```

3. **컨테이너에서 다시 열기 (Reopen in Container)**
   * VS Code 실행 시 우측 하단에 알림창이 뜨면 **"Reopen in Container"**를 클릭합니다.
   * 또는, 좌측 하단 초록색 아이콘(><) 클릭 -> **"Reopen in Container"** 선택.

4. **자동 설치 대기**
   * 컨테이너가 빌드되고 실행되면 `poetry install`이 자동으로 수행됩니다.
   * 터미널이 열리고 입력이 가능해지면 준비 완료입니다.

## 💻 개발 가이드 (Development)

컨테이너 내부 터미널에서는 이미 가상환경이 활성화되어 있습니다.

### 서버 실행
```bash
uvicorn main:app --reload
```

### 패키지 추가/관리
Poetry가 설치되어 있으므로 표준 명령어를 사용합니다.
```bash
# 패키지 추가
poetry add langchain-openai

# 의존성 업데이트
poetry update
```

## 📂 프로젝트 구조
```text
fastAPI-boilerPlate/
├── .devcontainer/      # Dev Container 설정 (Dockerfile, devcontainer.json)
├── main.py             # FastAPI 메인 애플리케이션
├── pyproject.toml      # 프로젝트 의존성 명세
├── poetry.lock         # 의존성 버전 잠금 파일
└── README.md
```